name: greet Nirlo

on:
  push:
    branches: [ master ]

jobs:
  greet_nirlo_job:
    runs_on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: checkout
        uses: action/checkout@v2
      - name: Greeting Nirlo 
        uses: ./actions/greet.yaml
        id: hello
        with:
          who-to-greet: 'Nirlo'
      - name: Get the output time
        run: echo "The time was ${{ steps.hello.outputs.time }}"